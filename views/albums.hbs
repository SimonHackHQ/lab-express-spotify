    <div class="list">
    {{#each albums}}
        <a class="album" href="/tracks/{{this.id}}"/>
        <h1>{{this.name}}</h1>
        <img src={{this.images.[0].url}}>
        </a>
    {{/each}}
    </div>
