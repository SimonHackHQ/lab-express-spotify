<div class="list">
    {{#each artists}}
        <a class="artist" href="/albums/{{this.id}}">
        <h1>{{this.name}}</h1>
        <img src={{this.images.[0].url}}>
        </a>
    {{/each}}
</div>